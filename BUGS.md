# Bug 记录约定

本文件用于在没有 Issue 系统或离线排查时记录待处理问题。每个问题应先能稳定复现，才进入修复；
修复后必须补充自动回归或 `tests/README.md` 中可执行的手工验收步骤。

## 严重度

| 等级 | 含义 | 处理原则 |
| --- | --- | --- |
| P0 | 安全边界失效、数据泄露、无法启动或大范围不可用 | 立即止损并优先修复 |
| P1 | 核心阅读、来源访问、版本浏览或搜索不可用 | 下一个稳定性迭代处理 |
| P2 | 有明确替代路径的功能错误、明显体验缺陷 | 排入后续小步迭代 |
| P3 | 文案、样式或低频边缘问题 | 汇总后择机处理 |

## 记录模板

```md
### BUG-YYYYMMDD-序号：一句话描述

- 状态：待确认 / 已复现 / 修复中 / 已验证 / 不修复
- 严重度：P0 / P1 / P2 / P3
- 发现版本与环境：应用版本、浏览器、操作系统、来源类型（GitHub / Bitbucket / 本地文件夹 / 本地 Git）
- 前置数据：仓库、scope、ref；不得记录访问令牌或私密内容
- 复现步骤：
  1. …
  2. …
- 预期结果：…
- 实际结果：…
- 证据：控制台信息、截图或脱敏后的链接
- 回归覆盖：自动检查名称，或 `tests/README.md` 的验收项
- 修复说明：PR/Commit 与验证日期
```

## 当前条目

### BUG-20260808-001：搜索结果可能无法打开 GitHub Markdown 文档

- 状态：已验证（待发布站点复测）
- 严重度：P1
- 发现版本与环境：GitHub Pages 发布站点；Chrome；GitHub 来源 `vitejs/vite`，scope `docs/guide`
- 前置数据：在文档页完成全文索引。
- 复现步骤：
  1. 搜索 `plugin`。
  2. 点击 `Api Plugin` 搜索结果。
- 预期结果：打开并渲染 `docs/guide/api-plugin.md`。
- 实际结果：URL 已跳转，但正文显示 `Unexpected token '#', "# Plugin A"... is not valid JSON`。
- 证据：2026-08-08 发布站点画面巡检。
- 回归覆盖：发布站点的真实浏览器路径：打开示例文档 → 搜索 `plugin` → 打开 `Api Plugin`。
- 修复说明：资源读取不再以 raw media type 请求 GitHub Contents API；改为 JSON 元数据请求后使用独立下载 URL 读取资源，避免与 Markdown JSON 读取发生浏览器缓存响应类型冲突。离线 Provider 回归测试已覆盖该请求顺序；待发布站点复测。
